const BSCodeContainer = ({children}) => {
    return (
        <div>
            {/*top bar */}
            {/*left icon bar */}
            {/*explorer bar */}
            {children}
        </div>
    )
}